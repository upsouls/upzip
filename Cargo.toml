[package]
name = "upzip"
version = "1.0.2"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
walkdir = "2.5.0"
zip = {version = "8.4.0", features = [
    "aes-crypto",
    # "bzip2",
    # "xz",
    "deflate64",
    "deflate",
    "lzma",
    "time",
    "zstd",
]}
